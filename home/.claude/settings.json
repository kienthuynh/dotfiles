{
  "model": "opus",
  "statusLine": {
    "type": "command",
    "command": "jq -j '[.model.display_name, (.context_window.used_percentage | if . == null then empty else \"ctx: \\(round)% used\" end), ([(.rate_limits.five_hour.used_percentage | if . == null then empty else \"5h: \\(round)%\" end), (.rate_limits.seven_day.used_percentage | if . == null then empty else \"7d: \\(round)%\" end)] | if length == 0 then empty else join(\" · \") end)] | join(\" | \")'"
  },
  "theme": "dark-ansi",
  "effortLevel": "high"
}
